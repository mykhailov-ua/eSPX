# eSPX local stack. Default NET_MODE=host binds services on loopback ports documented in docs/DEVELOPMENT.md.
# Ingestion (trackers + nginx) and observability use host networking; stateful stores sit on the database network.

services:
  # Ingestion plane: stateless gnet trackers, one process per port (8181-8184)
  # SO_REUSEPORT is configured in the binary; host mode avoids Docker NAT on the hot path.
  tracker-0:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/tracker"]
    network_mode: "${NET_MODE:-host}"
    env_file:
      - .env
    environment:
      - SERVER_PORT=8181
      - METRICS_PORT=9101
      - GOMEMLIMIT=700MiB   # hard memory limit; triggers GC before OOM killer fires
      - GOGC=50             # aggressive GC target: trade CPU for lower heap steady-state
    volumes:
      - ./deploy/geoip:/deploy/geoip:ro   # MaxMind DBs for geo filter and bid floor lookup
      - espx_logs:/var/log/espx:rw        # mmap audit log segments written by tracker
    ulimits:
      nofile:
        # gnet opens one fd per TCP connection; 100 000 allows ~100 k concurrent connections per instance
        soft: 100000
        hard: 100000
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: ["CMD", "/tracker", "--health-probe", "http://127.0.0.1:8181/health"]
      interval: 5s
      timeout: 3s
      retries: 3
    restart: always
    depends_on:
      db:
        condition: service_healthy
      redis-0:
        condition: service_healthy
      redis-1:
        condition: service_healthy
      redis-2:
        condition: service_healthy
      redis-3:
        condition: service_healthy
      redis-4:
        condition: service_healthy
      redis-5:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Replica; same image and deps as tracker-0, different SERVER_PORT.
  tracker-1:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/tracker"]
    network_mode: "${NET_MODE:-host}"
    env_file:
      - .env
    environment:
      - SERVER_PORT=8182
      - METRICS_PORT=9102
      - GOMEMLIMIT=700MiB
      - GOGC=50
    volumes:
      - ./deploy/geoip:/deploy/geoip:ro
      - espx_logs:/var/log/espx:rw
    ulimits:
      nofile:
        soft: 100000
        hard: 100000
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: ["CMD", "/tracker", "--health-probe", "http://127.0.0.1:8182/health"]
      interval: 5s
      timeout: 3s
      retries: 3
    restart: always
    depends_on:
      db:
        condition: service_healthy
      redis-0:
        condition: service_healthy
      redis-1:
        condition: service_healthy
      redis-2:
        condition: service_healthy
      redis-3:
        condition: service_healthy
      redis-4:
        condition: service_healthy
      redis-5:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Replica; same image and deps as tracker-0, SERVER_PORT=8183.
  tracker-2:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/tracker"]
    network_mode: "${NET_MODE:-host}"
    env_file:
      - .env
    environment:
      - SERVER_PORT=8183
      - METRICS_PORT=9103
      - GOMEMLIMIT=700MiB
      - GOGC=50
    volumes:
      - ./deploy/geoip:/deploy/geoip:ro
      - espx_logs:/var/log/espx:rw
    ulimits:
      nofile:
        soft: 100000
        hard: 100000
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: ["CMD", "/tracker", "--health-probe", "http://127.0.0.1:8183/health"]
      interval: 5s
      timeout: 3s
      retries: 3
    restart: always
    depends_on:
      db:
        condition: service_healthy
      redis-0:
        condition: service_healthy
      redis-1:
        condition: service_healthy
      redis-2:
        condition: service_healthy
      redis-3:
        condition: service_healthy
      redis-4:
        condition: service_healthy
      redis-5:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Replica; same image and deps as tracker-0, SERVER_PORT=8184.
  tracker-3:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/tracker"]
    network_mode: "${NET_MODE:-host}"
    env_file:
      - .env
    environment:
      - SERVER_PORT=8184
      - METRICS_PORT=9104
      - GOMEMLIMIT=700MiB
      - GOGC=50
    volumes:
      - ./deploy/geoip:/deploy/geoip:ro
      - espx_logs:/var/log/espx:rw
    ulimits:
      nofile:
        soft: 100000
        hard: 100000
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: ["CMD", "/tracker", "--health-probe", "http://127.0.0.1:8184/health"]
      interval: 5s
      timeout: 3s
      retries: 3
    restart: always
    depends_on:
      db:
        condition: service_healthy
      redis-0:
        condition: service_healthy
      redis-1:
        condition: service_healthy
      redis-2:
        condition: service_healthy
      redis-3:
        condition: service_healthy
      redis-4:
        condition: service_healthy
      redis-5:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Edge LB: /track/* to trackers, /admin/* to management. Lua parses bodies for shard routing and blacklist cache.
  nginx:
    image: openresty/openresty:alpine
    network_mode: "${NET_MODE:-host}"
    environment:
      - REDIS_HOST=127.0.0.1
      - REDIS_PORT=6479
      - REDIS_PASS=${REDIS_PASSWORD}
      - REDIS_ADDRS=127.0.0.1:6479,127.0.0.1:6480,127.0.0.1:6481,127.0.0.1:6482,127.0.0.1:6483,127.0.0.1:6484
      - MANAGEMENT_URL=http://127.0.0.1:8188
    volumes:
      - ./deploy/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./deploy/nginx/nginx.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro
      - ./deploy/nginx/lua:/etc/nginx/lua:ro
    depends_on:
      tracker-0:
        condition: service_healthy
      tracker-1:
        condition: service_healthy
      tracker-2:
        condition: service_healthy
      tracker-3:
        condition: service_healthy
    ulimits:
      nofile:
        soft: 100000
        hard: 100000
    deploy:
      resources:
        limits:
          memory: 1G
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Settlement: Redis stream consumer groups -> Postgres (ledger/stats) + ClickHouse (telemetry).
  processor:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/processor"]
    network_mode: "${NET_MODE:-host}"
    env_file:
      - .env
    volumes:
      - espx_logs:/var/log/espx:rw
      - ./deploy/geoip:/deploy/geoip:rw
    environment:
      - DB_DSN=${DB_DSN}
      - REDIS_ADDRS=127.0.0.1:6479,127.0.0.1:6480,127.0.0.1:6481,127.0.0.1:6482,127.0.0.1:6483,127.0.0.1:6484
      - CH_DSN=${CH_DSN}
      - PROCESSOR_PORT=${PROCESSOR_PORT:-8186}
      - SERVER_PORT=${PROCESSOR_PORT:-8186}
      - GEOIP_DB_PATH=/deploy/geoip/GeoLite2-Country.mmdb
      - GEOIP_UPDATER_ENABLED=${GEOIP_UPDATER_ENABLED:-false}
      - MAXMIND_LICENSE_KEY=${MAXMIND_LICENSE_KEY:-}
      - DB_PROCESSOR_MAX_CONNS=${DB_PROCESSOR_MAX_CONNS}
      - ENV=${ENV:-development}
      - GOMEMLIMIT=1500MiB  # processor holds larger in-flight batches; needs wider heap
      - GOGC=100            # standard GC pace; processor is not latency-critical
    healthcheck:
      test: ["CMD", "/processor", "--health-probe", "http://127.0.0.1:8186/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 2G
    restart: always
    depends_on:
      db:
        condition: service_healthy
      redis-0:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Control plane: gRPC auth (Argon2id + PASETO). Single Redis shard is enough for session/lockout keys.
  auth:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/auth"]
    network_mode: "${NET_MODE:-host}"
    env_file:
      - .env
    environment:
      - AUTH_SERVER_PORT=51051
      - DB_DSN=${DB_DSN}
      - REDIS_ADDRS=127.0.0.1:6479
    deploy:
      resources:
        limits:
          memory: 512M
    restart: always
    depends_on:
      db:
        condition: service_healthy
      redis-0:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Control plane: REST admin API, settlement gRPC, outbox workers, pacing/schedule/recon background jobs.
  management:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/management"]
    network_mode: "${NET_MODE:-host}"
    profiles:
      - chaos
      - fraud-scorer
    env_file:
      - .env
    environment:
      - MANAGEMENT_PORT=8188
      - SETTLEMENT_SERVER_PORT=51053
      - AUTH_SERVER_PORT=51051
      - AUTH_SERVER_HOST=127.0.0.1
      - BILLING_SERVER_HOST=127.0.0.1
      - BILLING_SERVER_PORT=51054
      - PAYMENT_SERVER_HOST=127.0.0.1
      - PAYMENT_SERVER_PORT=51052
      - NOTIFIER_SERVER_HOST=127.0.0.1
      - NOTIFIER_PORT=8085
      - ALERTMANAGER_WEBHOOK_ENABLED=${ALERTMANAGER_WEBHOOK_ENABLED:-false}
      - ALERTMANAGER_WEBHOOK_TOKEN=${ALERTMANAGER_WEBHOOK_TOKEN:-}
      - BLACKLIST_JANITOR_ENABLED=${BLACKLIST_JANITOR_ENABLED:-true}
      - DB_DSN=${DB_DSN}
      - REDIS_ADDRS=127.0.0.1:6479
    deploy:
      resources:
        limits:
          memory: 768M
    restart: always
    depends_on:
      auth:
        condition: service_started
      db:
        condition: service_healthy
      redis-0:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Control plane: async notification delivery (Telegram, Slack, SMTP, SMS).
  notifier:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/notifier"]
    network_mode: "${NET_MODE:-host}"
    profiles:
      - chaos
    env_file:
      - .env
    environment:
      - NOTIFIER_PORT=8085
      - DB_DSN=${DB_DSN}
      - GOMEMLIMIT=400MiB
      - NOTIFIER_WORKER_CONCURRENCY=1
      - NOTIFIER_GROUP_PARALLELISM=2
    deploy:
      resources:
        limits:
          memory: 512M
    restart: always
    depends_on:
      db:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Off-path IVT botnet detection: ClickHouse analytics → fraud blacklist via management API.
  ivt-detector:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/ivt-detector"]
    network_mode: "${NET_MODE:-host}"
    profiles:
      - chaos
    env_file:
      - .env
    environment:
      - IVT_DETECTOR_ENABLED=true
      - CH_DSN=${CH_DSN}
      - DB_DSN=${DB_DSN}
      - MANAGEMENT_URL=http://127.0.0.1:8188
      - ADMIN_API_KEY=${ADMIN_API_KEY}
    deploy:
      resources:
        limits:
          memory: 256M
    restart: always
    depends_on:
      db:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
      management:
        condition: service_started
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"

  fraud-scorer:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/fraud-scorer"]
    network_mode: "${NET_MODE:-host}"
    profiles:
      - fraud-scorer
    env_file:
      - .env
    environment:
      - FRAUD_SCORING_ENABLED=true
      - FRAUD_SCORER_STANDALONE=true
      - CH_DSN=${CH_DSN}
      - DB_DSN=${DB_DSN}
      - MANAGEMENT_URL=http://127.0.0.1:8188
      - ADMIN_API_KEY=${ADMIN_API_KEY}
    volumes:
      - ./var/fraudscore/artifacts:/var/fraudscore/artifacts:rw
    deploy:
      resources:
        limits:
          memory: 512M
    restart: always
    depends_on:
      db:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
      management:
        condition: service_started
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Control plane: payment intents, webhooks, settlement outbox (calls management settlement gRPC).
  payment:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/payment"]
    network_mode: "${NET_MODE:-host}"
    profiles:
      - chaos
    env_file:
      - .env
    environment:
      - PAYMENT_SERVER_PORT=51052
      - PAYMENT_WEBHOOK_PORT=8187
      - SETTLEMENT_SERVER_HOST=127.0.0.1
      - SETTLEMENT_SERVER_PORT=51053
      - PAYMENT_DB_DSN=${PAYMENT_DB_DSN}
    deploy:
      resources:
        limits:
          memory: 512M
    restart: always
    depends_on:
      db-payment:
        condition: service_healthy
      management:
        condition: service_started
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Control plane: monthly invoice generation from balance_ledger aggregates.
  billing:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/billing"]
    network_mode: "${NET_MODE:-host}"
    profiles:
      - chaos
    env_file:
      - .env
    environment:
      - BILLING_SERVER_PORT=51054
      - DB_DSN=${DB_DSN}
    deploy:
      resources:
        limits:
          memory: 512M
    restart: always
    depends_on:
      db:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Relational source of truth: campaigns, ledger, outbox, audit. Tuned for many short tracker connections.
  db:
    image: postgres:${POSTGRES_VERSION}
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    ports:
      - "${DB_PORT}:${DB_PORT}"
    command:
      # max_connections: sized for 4 tracker replicas x 4 conns + processor x 16 + management + auth headroom
      # shared_buffers: ~25% of container memory limit (2G)
      # work_mem: per-sort/hash allocation; 64MB per connection x 600 connections worst case = 38G virtual
      # maintenance_work_mem: index builds and vacuum
      # effective_cache_size: planner assumption for page cache; ~75% of container memory
      ["postgres", "-p", "${DB_PORT}", "-c", "max_connections=600", "-c", "shared_buffers=1GB", "-c", "work_mem=64MB", "-c", "maintenance_work_mem=256MB", "-c", "effective_cache_size=3GB"]
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - database
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -p ${DB_PORT} -U ${DB_USER} -d ${DB_NAME}"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}
    deploy:
      resources:
        limits:
          memory: 2G
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Payment-only Postgres: checkout intents, webhooks, settlement outbox (compliance isolation from ads ledger).
  db-payment:
    image: postgres:${POSTGRES_VERSION}
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${PAYMENT_DB_USER}
      - POSTGRES_PASSWORD=${PAYMENT_DB_PASSWORD}
      - POSTGRES_DB=${PAYMENT_DB_NAME}
    ports:
      - "${PAYMENT_DB_PORT}:${PAYMENT_DB_PORT}"
    command:
      ["postgres", "-p", "${PAYMENT_DB_PORT}", "-c", "max_connections=100", "-c", "shared_buffers=256MB", "-c", "work_mem=16MB", "-c", "maintenance_work_mem=64MB", "-c", "effective_cache_size=768MB"]
    volumes:
      - db_payment_data:/var/lib/postgresql/data
    networks:
      - database
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -p ${PAYMENT_DB_PORT} -U ${PAYMENT_DB_USER} -d ${PAYMENT_DB_NAME}"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}
    deploy:
      resources:
        limits:
          memory: 1G
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Redis shards (client-side StaticSlotSharder, ports 6479-6484)
  # AOF everysec + noeviction: budget/pacing keys must not be silently dropped under memory pressure.
  redis-0:
    image: redis:${REDIS_VERSION}
    env_file:
      - .env
    ports:
      - "6479:6379"
    command: ["redis-server", "--port", "6379", "--requirepass", "${REDIS_PASSWORD}",
              "--appendonly", "yes", "--appendfsync", "everysec",
              "--maxmemory-policy", "noeviction"]
    volumes:
      - redis_0_data:/data
    deploy:
      resources:
        limits:
          memory: 768M
    networks:
      - database
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6379", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}

  redis-1:
    image: redis:${REDIS_VERSION}
    env_file:
      - .env
    ports:
      - "6480:6379"
    command: ["redis-server", "--port", "6379", "--requirepass", "${REDIS_PASSWORD}",
              "--appendonly", "yes", "--appendfsync", "everysec",
              "--maxmemory-policy", "noeviction"]
    volumes:
      - redis_1_data:/data
    deploy:
      resources:
        limits:
          memory: 768M
    networks:
      - database
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6379", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}

  redis-2:
    image: redis:${REDIS_VERSION}
    env_file:
      - .env
    ports:
      - "6481:6379"
    command: ["redis-server", "--port", "6379", "--requirepass", "${REDIS_PASSWORD}",
              "--appendonly", "yes", "--appendfsync", "everysec",
              "--maxmemory-policy", "noeviction"]
    volumes:
      - redis_2_data:/data
    deploy:
      resources:
        limits:
          memory: 768M
    networks:
      - database
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6379", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}

  redis-3:
    image: redis:${REDIS_VERSION}
    env_file:
      - .env
    ports:
      - "6482:6379"
    command: ["redis-server", "--port", "6379", "--requirepass", "${REDIS_PASSWORD}",
              "--appendonly", "yes", "--appendfsync", "everysec",
              "--maxmemory-policy", "noeviction"]
    volumes:
      - redis_3_data:/data
    deploy:
      resources:
        limits:
          memory: 768M
    networks:
      - database
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6379", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}

  redis-4:
    image: redis:${REDIS_VERSION}
    env_file:
      - .env
    ports:
      - "6483:6379"
    command: ["redis-server", "--port", "6379", "--requirepass", "${REDIS_PASSWORD}",
              "--appendonly", "yes", "--appendfsync", "everysec",
              "--maxmemory-policy", "noeviction"]
    volumes:
      - redis_4_data:/data
    deploy:
      resources:
        limits:
          memory: 512M
    networks:
      - database
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6379", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}

  redis-5:
    image: redis:${REDIS_VERSION}
    env_file:
      - .env
    ports:
      - "6484:6379"
    command: ["redis-server", "--port", "6379", "--requirepass", "${REDIS_PASSWORD}",
              "--appendonly", "yes", "--appendfsync", "everysec",
              "--maxmemory-policy", "noeviction"]
    volumes:
      - redis_5_data:/data
    deploy:
      resources:
        limits:
          memory: 512M
    networks:
      - database
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6379", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: ${HEALTHCHECK_INTERVAL}
      timeout: ${HEALTHCHECK_TIMEOUT}
      retries: ${HEALTHCHECK_RETRIES}

  # Columnar event store. Processor batches inserts; insert_deduplicate handles at-least-once from streams.
  clickhouse:
    image: clickhouse/clickhouse-server:${CH_VERSION}
    env_file:
      - .env
    ports:
      - "${CH_PORT}:${CH_PORT}"
      - "${CH_HTTP_PORT}:8123"
    ulimits:
      nofile:
        soft: 262144  # ClickHouse requires high fd limits for part merges and replication connections
        hard: 262144
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ./deploy/clickhouse/config.yaml:/etc/clickhouse-server/config.d/memory_limit.yaml:ro
      - ./deploy/clickhouse/users.yaml:/etc/clickhouse-server/users.d/custom_users.yaml:ro
      - ./deploy/clickhouse/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - ./deploy/clickhouse/recon_materialized_views.sql:/docker-entrypoint-initdb.d/02_recon_materialized_views.sql:ro
    deploy:
      resources:
        limits:
          memory: 4G
    networks:
      - database
      - monitoring
    environment:
      - CLICKHOUSE_DB=${CH_NAME}
      - CLICKHOUSE_USER=${CH_USER}
      - CLICKHOUSE_PASSWORD=${CH_PASSWORD}
      - CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8123/ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"


  # Observability (host network, scrapes tracker/mgmt/processor metrics on loopback)
  prometheus:
    image: prom/prometheus:latest
    network_mode: "${NET_MODE:-host}"
    command: ["--config.file=/etc/prometheus/prometheus.yaml", "--web.listen-address=:9190"]
    volumes:
      - ./deploy/monitoring/prometheus.yaml:/etc/prometheus/prometheus.yaml:ro
      - ./deploy/monitoring/prometheus.rules.yaml:/etc/prometheus/prometheus.rules.yaml:ro
    deploy:
      resources:
        limits:
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"

  grafana:
    image: grafana/grafana:latest
    network_mode: "${NET_MODE:-host}"
    # Anonymous admin is dev-only; lock down before any non-local exposure.
    volumes:
      - ./deploy/monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
    environment:
      - GF_SERVER_HTTP_PORT=3100
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
    deploy:
      resources:
        limits:
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"

  alertmanager:
    image: prom/alertmanager:latest
    network_mode: "${NET_MODE:-host}"
    # Routes firing alerts to management Alertmanager webhook adapter on :8188.
    command: ["--config.file=/etc/alertmanager/alertmanager.yaml", "--web.listen-address=:9093"]
    volumes:
      - ./deploy/monitoring/alertmanager.yaml:/etc/alertmanager/alertmanager.yaml:ro
    deploy:
      resources:
        limits:
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"

  # Webhook proxy: Alertmanager JSON -> Telegram Bot API HTML messages.
  alertmanager-telegram:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["/alertmanager-telegram"]
    network_mode: "${NET_MODE:-host}"
    environment:
      - PROXY_PORT=8222
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-YOUR_TELEGRAM_BOT_TOKEN_PLACEHOLDER}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID:-YOUR_TELEGRAM_CHAT_ID_PLACEHOLDER}
    deploy:
      resources:
        limits:
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"

  # Warm-tier downsampling of rotated audit segments (local FS MVP). Profile "tools" only.
  log-compactor:
    build:
      context: .
      dockerfile: Dockerfile.log-compactor
    volumes:
      - espx_logs:/var/log/espx:ro
      - log_compactor_state:/var/lib/espx:rw
    environment:
      - LOG_COMPACTOR_BACKEND=local
      - LOG_COMPACTOR_SOURCE_DIR=/var/log/espx
      - LOG_COMPACTOR_WARM_DIR=/var/lib/espx/log-store/warm
      - LOG_COMPACTOR_HOT_MIN_AGE_H=24
      - LOG_COMPACTOR_WARM_SAMPLE_RATE=1000
      - LOG_COMPACTOR_WORK_INTERVAL_H=1
      - LOG_COMPACTOR_METRICS_PORT=9190
      - CH_DSN=${CH_DSN}
      - LOG_COMPACTOR_COLD_WARM_MIN_AGE_D=7
      - LOG_COMPACTOR_COLD_WORK_INTERVAL_H=24
    profiles:
      - tools
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Off-path rsync of rotated .log.zst.ready segments to remote storage. Profile "tools" only.
  log-evacuator:
    build:
      context: .
      dockerfile: Dockerfile.log-evacuator
    volumes:
      - espx_logs:/var/log/espx:rw
      - ${STORAGE_BOX_SSH_KEY_PATH:-~/.ssh/id_rsa}:/root/.ssh/id_rsa:ro
    environment:
      - LOG_DIR=/var/log/espx
      - STORAGE_BOX_HOST=${STORAGE_BOX_HOST}
      - STORAGE_BOX_USER=${STORAGE_BOX_USER}
      - STORAGE_BOX_PATH=${STORAGE_BOX_PATH:-/}
      - RSYNC_BWLIMIT=${RSYNC_BWLIMIT:-5000}
    profiles:
      - tools
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  frontend:   # reserved; not wired in this compose file
  backend:    # reserved; not wired in this compose file
  database:   # postgres, redis, clickhouse
  monitoring: # clickhouse metrics export

volumes:
  db_data:           # postgres data dir (ads, auth, billing, notifier)
  db_payment_data:   # payment schema only
  clickhouse_data:   # clickhouse parts and metadata
  redis_0_data:      # per-shard AOF/RDB; keys are not shared across shards
  redis_1_data:
  redis_2_data:
  redis_3_data:
  redis_4_data:
  redis_5_data:
  espx_logs:           # shared mmap audit logs from all trackers/processor
  log_compactor_state: # warm-tier compactor checkpoints and output
