# Standalone fraud-scorer worker and weekly retraining CronJob.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-scorer
  namespace: espx
  labels:
    app.kubernetes.io/name: fraud-scorer
    app.kubernetes.io/component: cold-path
    app.kubernetes.io/part-of: espx
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fraud-scorer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fraud-scorer
        app.kubernetes.io/component: cold-path
        app.kubernetes.io/part-of: espx
    spec:
      containers:
        - name: fraud-scorer
          image: ad-event-processor:latest
          imagePullPolicy: IfNotPresent
          command: ["/fraud-scorer"]
          envFrom:
            - configMapRef:
                name: espx-env
            - secretRef:
                name: espx-secrets
          env:
            - name: FRAUD_SCORER_STANDALONE
              value: "true"
            - name: FRAUD_SCORING_ENABLED
              value: "true"
          volumeMounts:
            - name: ml-artifacts
              mountPath: /var/fraudscore/artifacts
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 512Mi
              cpu: 500m
      volumes:
        - name: ml-artifacts
          persistentVolumeClaim:
            claimName: ml-artifacts-pvc

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: fraud-scorer-retrain
  namespace: espx
  labels:
    app.kubernetes.io/name: fraud-scorer-retrain
    app.kubernetes.io/component: cold-path
    app.kubernetes.io/part-of: espx
spec:
  schedule: "0 0 * * 0"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: retrain
              image: ad-event-processor:latest
              imagePullPolicy: IfNotPresent
              command: ["/bin/bash", "-c", "/scripts/fraud-scoring/train.sh"]
              envFrom:
                - configMapRef:
                    name: espx-env
                - secretRef:
                    name: espx-secrets
              volumeMounts:
                - name: ml-artifacts
                  mountPath: /var/fraudscore/artifacts
          restartPolicy: OnFailure
          volumes:
            - name: ml-artifacts
              persistentVolumeClaim:
                claimName: ml-artifacts-pvc
