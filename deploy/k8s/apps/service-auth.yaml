apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: espx
  labels:
    app.kubernetes.io/name: auth
    app.kubernetes.io/part-of: espx
spec:
  selector:
    app.kubernetes.io/name: auth
  ports:
    - name: grpc
      port: 51051
      targetPort: grpc
