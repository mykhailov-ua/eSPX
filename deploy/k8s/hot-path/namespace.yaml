# Hot-path namespace. hostNetwork pods bind host ports directly (no CNI NAT on /track).
apiVersion: v1
kind: Namespace
metadata:
  name: espx-edge
  labels:
    app.kubernetes.io/part-of: espx
    app.kubernetes.io/component: hot-path
